<odoo>
    <!-- Manual Sync Action for Payments -->
    <record id="action_sync_payments_to_project_income" model="ir.actions.server">
        <field name="name">Sync All Payments to Project Income</field>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="state">code</field>
        <field name="code">
count = model.cron_sync_payments_to_project_income()
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'type': 'success',
        'message': 'Synced %d payments to project income' % count,
        'sticky': False,
    }
}
        </field>
    </record>

    <!-- Add menu item for manual sync -->
    <menuitem id="menu_sync_payments" 
              name="Sync Payments to Income" 
              parent="project_extended.menu_project_extended_root" 
              action="action_sync_payments_to_project_income"
              sequence="20"/>
</odoo>
