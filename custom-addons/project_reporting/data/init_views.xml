<odoo>
    <data>
        <!-- Initialize project dashboard model and create the SQL view -->
        <record id="init_project_dashboard" model="ir.cron">
            <field name="name">Initialize Project Dashboard</field>
            <field name="model_id" ref="base.model_ir_cron"/>
            <field name="state">code</field>
            <field name="code">
# Initialize the project.dashboard model
env['project.dashboard'].init()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">1</field>
            <field name="active">False</field>
        </record>
    </data>
</odoo>
